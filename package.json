{
  "name": "cypress",
  "version": "1.0.0",
  "description": "cypress automation by via",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "via",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.5.3"
  }
}
